{
  "name": "multi-regl-transform",
  "version": "1.0.0",
  "description": "browserify transform to transparently convert the [regl][1] API to use [multi-regl][2]",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "static-module": "^1.3.1"
  }
}
